/// Contains the SqlTable and SqlEnum traits for structs and enums respectively, which describe the
/// table/type name and creation query
pub mod traits;

/// Contains the user struct and auxillary information (like cookies, SSH keys, and GPG keys)
pub mod user;

pub mod listing;
